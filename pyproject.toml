[project]
name = "mcpserver"
version = "0.1.0"
description = "MCP Server for tests"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "pydantic>=2.11.4",
    "sqlalchemy>=2.0.40",
]
